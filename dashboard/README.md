# Smallville dashboard

Next.js dashboard for [Smallville](../README.md) — create and manage agents/locations, control simulation speed, and watch agent diaries and conversations unfold in real time.

```
npm install
npm run dev
```

Talks to a Smallville server running on `localhost:8080` by default (see the [root README](../README.md#getting-started) for running the server).
