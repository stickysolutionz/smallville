options.txt
glove.840B.300d.txt
.classpath
.project
/logs/
/.settings
*.log
*.prefs
.next
out
pages
node_modules
/backups/

# Runtime data written by the backend. These paths are resolved relative to
# the JVM working directory, which is the repo root when the jar is run as
# documented in the README but smallville/ when run from the module dir - so
# these are deliberately not anchored with a leading slash.
story-data/
location-images/
world-data/

# Ad-hoc API response dumps from manual curl testing
story_*.json

# Local runtime override. SmallvilleConfig reads the working directory before
# the copy inside the jar, so this is per-machine tuning rather than something
# to ship - the committed defaults live in smallville/src/main/resources.
/config.yaml
